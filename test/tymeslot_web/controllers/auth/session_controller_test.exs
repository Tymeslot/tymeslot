# These tests were testing non-existent controller endpoints.
# Password reset functionality is handled by AuthLive (LiveView), not controllers.
# Brutal test audit: DELETE tests that test non-existent functionality.
