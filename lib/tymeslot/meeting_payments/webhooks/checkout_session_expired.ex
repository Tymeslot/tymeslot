defmodule Tymeslot.MeetingPayments.Webhooks.CheckoutSessionExpired do
  @moduledoc """
  Placeholder for the `checkout.session.expired` handler. The real
  implementation arrives in Chunk 4 / Task 4.3.
  """

  @spec handle(map()) :: :ok | {:error, term()}
  def handle(_event), do: :ok
end
